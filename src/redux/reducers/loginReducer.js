let loginState = {
  email: "",
  name: "",
  logged: false
};

/**
 |--------------------------------------------------
 | Login
 |--------------------------------------------------
 */
export default (state = loginState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
